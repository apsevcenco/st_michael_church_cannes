name: Backend keep-alive

on:
  schedule:
    - cron: "*/10 * * * *"
  workflow_dispatch: {}

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping backend healthz
        run: |
          curl -fsS --max-time 30 https://st-michael-church-cannes.onrender.com/healthz
